 * 
 * --------------------------------------------------------------------------------------------------------------------------------------------------------------------------
 * 
 * Consulta inicial es el año de operaciones, en php lo separo en grupo de semanas, sumando la primera cuatro semanas serian el mes
 * Una sola consulta es el año se separa por semana y se separa por PHP desde un array
 * Una tabla transitorias de usuarios que esten en red flag (user_id, nombre, sumatoria de los montos (dato flotante))
 * fecha desde la mas reciente a la mas vieja 
 * Crear en banker un metodo estatico donde reciba un usuario y su monto y haga el cruce de datos donde verifique el datos
 * si uno de los limites esta roto el usuario se marcara como red flag
 * es decir se envia a otra tabla donde se alamcene el usuario_id, nombre, montos, is_blocked, fecha de bloqueo, fecha de desbloqueo, user_id de quien desbloqueo
 * si el usuario esta bloqueado se vera en la lista de usuarios de procesos ya creado
 * el motodo en banker se llamara en los perfiles del usuario y en sus operaciones

 
 
     int value, contX = 0, contY = 0;
        
        while(contX < m.length){
            while(contY < m[contX].length){
                value = Integer.parseInt(values.getText());
                if(value < 9999 && value > -9999){
                    m[contX][contY] =  value;
                    text_area.append("Valor en la posición: [" +contX+ "][" +contY+ "]: " + m[contX][contY] + "\n");
                    values.setText("");
                    values.requestFocus();
                    contY++;
                }else{
                    text_area.append("Por favor ingrese un numero no mayor a 4 digitos \n");
                    values.setText("");
                    values.requestFocus();
                }
            }
            contX++;
 	}
        
        values.setEnabled(false);

        
// elimar correo de de outgoing
